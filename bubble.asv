function  a= bubble(a)
% Ã°Åİ³ÌĞò
for i=1:size(a,2)-1
    for j=i:size(a,2)-1
        if(a(j)>a(j+1))
            tem=a(j);
            a(j)=a(j+1);
            a(j+1)=tem;
        end
    end
end
end
